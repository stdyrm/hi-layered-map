import styled from "@emotion/styled";

const Header = styled.header({
	display: "flex",
	alignItems: "baseline",
	justifyContent: "space-between",
	padding: "1rem",
	boxShadow: "1px 0px 16px 4px #CDC9E3",
});

export default Header;
